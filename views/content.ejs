<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <%- include('header.ejs') %>
</head>
<body>

<div class="d-flex">
    <%- include('sidebar.ejs') %>

    <div class="container mt-5">
        <h3 class="mb-4">Title: <%= title %></h3>
        <h5 class="mb-3">Read the essay below:</h5>
        <h5 class="mb-3"><%= content %></h5>

        <a href="/<%= title.toLowerCase().replace(/\s+/g, '-') %>/edit" class="btn btn-primary mt-3">Edit</a>
        <a href="/<%= title.toLowerCase().replace(/\s+/g, '-') %>/delete" class="btn btn-danger mt-3">Delete</a>
    </div>
</div>

<%- include('footer.ejs') %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
